name: Workflow B

on:
 workflow_run:
    workflows: ["Workflow A"]
    types:
      - completed
jobs:
  build:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Check am
        run: |
         echo "it got to me"
         - name: Use Environment Variable
      - name: Use variable
        run: |
          echo "Environment Variable from Workflow A: ${{ needs.workflow_a.outputs.my_variable }}"
      # Your build steps here
