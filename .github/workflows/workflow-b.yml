name: Workflow B

on:
 workflow_run:
    workflows: ["Workflow A"]
    types:
      - completed
jobs:
  build:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Check am
        run: |
         echo "it got to me"
      - name: Download Artifact
        uses: actions/download-artifact@v2
        with:
          name: my-artifact
          
      - name: Read Environment Variable
        run: |
          echo "Environment Variable from Workflow A: $(cat my_variable.txt)"
